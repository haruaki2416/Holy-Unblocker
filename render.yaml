services:
  - type: web
    name: cloud-app
    env: node
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: node backend.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
